# [적용] 데이터 정합성 수정 — 회신 반영 결과

일자: 2026-07-28
근거: `데이터정합성_수정_회신.md` (회신에서 "1번 A/C 결정" 요청 → **C안 선택**)
적용 범위: `collect_ga4.py`, `analyze.py`, `common.py`, `build_onepager.py`

> ⚠️ **실행 검증을 하지 못했습니다.** 작업 환경에 Python 이 없어 문법 검사·실행 테스트를 돌리지 못했고, 변경부 정독으로만 확인했습니다. **첫 파이프라인 실행 결과를 반드시 확인해 주세요.** 되돌리는 방법은 맨 아래 6장에 있습니다.

---

## 1. 회신 항목별 적용 결과

| 회신 | 항목 | 적용 |
|---|---|---|
| 2장 | 참여시간 합산 오류 | ✅ 적용 |
| 3장 | 7일 평균 → 날짜 구간 기준 | ✅ 적용 (회신 코드 그대로) |
| 4장 | GA4 수집 시점 — **C안** | ✅ 적용 (+ 회신 6장 조건 2개 추가 구현) |
| 5장 | 원페이지 당일 제외 | ✅ 적용 (회신 코드 그대로) |
| 6장 | 과거 33일 백필 | ❌ **미적용** — 4장 참고 |

### 1번 A/C 결정 근거

**C안을 선택했습니다.**

2026-07-28에 스케줄이 처음 자동 발동했는데, 예정(00:00 UTC / 09:00 KST)보다 **3시간 23분 늦게** 03:23 UTC 에 실행됐습니다. 그 덕에 7/27 이탈률이 **32.7%** 로 정상 저장됐습니다 (모바일 31.0% / 데스크톱 34.3%).

확정 경계는 회신의 "약 19시간"보다 이릅니다 — 실측으로 **+9시간은 비정상(99%), +12.4시간은 정상(32.7%)** 입니다.

즉 A안(실행 시각 이동)으로도 해결되지만, **cron 지연 폭을 통제할 수 없어** 실행 시각에 의존하는 방식은 불안정합니다. C안은 실행 시각과 무관하게 동작합니다.

> 별건 제안: cron 을 정각(`0 0 * * *`)에서 옮기면 지연이 줄어듭니다. 정각 UTC 는 GitHub Actions 에서 가장 혼잡한 슬롯입니다.

---

## 2. `collect_ga4.py`

### 2-1. 참여시간 합산 (회신 2장)

```python
# before
"user_engagement_duration": weighted("user_engagement_duration"),
# after
"user_engagement_duration": sum(d.get("user_engagement_duration", 0) or 0 for d in devices),
```

`bounce_rate` · `avg_session_duration` 은 비율·평균 지표이므로 가중평균을 유지했습니다.

### 2-2. 확정본 수집 + 자기 치유 (회신 4장 C안)

```python
SETTLE_DAYS = 2      # D-2 부터 확정본으로 본다
BACKFILL_DAYS = 4    # D-2 ~ D-5 를 매일 다시 받아 덮어쓴다

def main():
    property_id = common.env("GA4_PROPERTY_ID", required=True)
    today = date.today()
    for back in range(SETTLE_DAYS, SETTLE_DAYS + BACKFILL_DAYS):
        d = today - timedelta(days=back)
        try:
            data = fetch(property_id, d)
        except Exception as e:
            print(f"[GA4] {d} 수집 실패 (건너뜀): {str(e)[:120]}")
            continue
        ...
```

- 회신 4장에서 지적하신 **`datetime` import 누락**을 반영해 `from datetime import date, timedelta` 를 추가했습니다.
- 하루가 실패해도 나머지 날짜는 계속 받도록 `try/except` 를 넣었습니다 (4일치를 순회하므로 한 날짜 실패로 전체가 멈추면 손해가 큼).

---

## 3. `analyze.py`

회신의 스케치를 적용하고, **회신 6장의 표기 조건 2개를 구현했습니다.** 스케치만 넣으면 매일 리포트의 독자 반응이 "데이터 없음" 으로 나가기 때문입니다.

### 3-1. 재조회분을 과거 레코드에 반영 (자기 치유)

```python
GA4_BACKFILL_DAYS = 4

def refresh_ga4_in_records(d):
    """재조회분(D-2~D-5)을 기존 일별 레코드의 ga4 키에 반영."""
```

- **AdSense 수치는 건드리지 않습니다** (`ga4` 키만 교체 → 수익 금액 불변)
- 값이 실제로 달라졌을 때만 저장하므로 불필요한 커밋 diff 가 생기지 않습니다
- `main()` 의 **가장 앞**에서 실행됩니다. 이후 비교·7일평균이 확정값 기준이 되도록 하기 위한 순서입니다

### 3-2. 독자 반응은 확정본 레코드 안에서만 계산

```python
def latest_settled_record(d):
    """GA4 가 확정된 가장 최근 일별 레코드를 (날짜, 레코드) 로 반환."""

def visit_rpm(rec):
    """방문당 수익(1,000명당) — 같은 레코드 안의 earnings ÷ sessions 로만 계산."""
```

회신 6장 조건 이행:

| 회신 요구 | 구현 |
|---|---|
| "독자 반응 섹션에 기준일 D-2 (확정) 명시" | Jandi 리포트에 `※ 기준일 2026-07-26 — GA4 확정본 (수익은 2026-07-27)` 한 줄 추가. 일별 레코드에 `reader_date` 키 저장 |
| "방문당 수익은 같은 날짜끼리만" | `visit_rpm()` 이 **확정본 레코드 내부의** earnings ÷ sessions 만 사용 |

### 3-3. 인사이트도 확정본 기준으로

`build_prompt()` / `fallback_insight()` 가 D-1 의 빈 GA4 를 읽어 "이탈률: 데이터 없음" 만 보게 되는 문제가 있어, 확정본 비교값을 넘기도록 했습니다. 프롬프트에는 기준일 안내 한 줄이 들어갑니다.

```
[아래 독자 지표는 2026-07-26 기준 — GA4 확정본]
- 이탈률: 42.10% (전일 ..., 7일평균 ...)
```

---

## 4. 미적용 — 과거 33일 백필 (회신 6장)

**하지 않았습니다.** `data/*.json` 33일치를 덮어쓰는 작업인데 실행 검증이 불가능한 상태라, 검증 없이 만든 스크립트로 진행하는 것은 위험하다고 판단했습니다.

실행 환경이 있는 쪽에서 진행 부탁드립니다. 회신에 적힌 전제 그대로가 맞습니다.

- `ga4` 키만 교체, AdSense 수치 불변
- `data/` 백업 또는 별도 브랜치 → PR
- 백필 후 이탈률 추세선과 과거 인사이트 문구가 바뀜 (그게 목적)

참고로 **일상 실행만으로도 최근 4일은 자동 치유됩니다**(3-1). 33일 전체를 되살리려면 일회성 스크립트가 필요합니다.

---

## 5. 알려진 부작용 — 대시보드 최신일 독자 데이터 공란

이번 변경으로 `data/{어제}.json` 의 `ga4` 는 **의도적으로 빈값**입니다 (아직 확정 전이므로 정상 동작).

그래서 **대시보드에서 최신 날짜를 고르면 "독자 데이터 없음"** 이 표시됩니다. 하루 전 날짜를 고르면 정상입니다.

`build_dashboard.py` 는 **일부러 수정하지 않았습니다** — 회신 범위가 아니고, 실행 검증 없이 화면 로직까지 손대면 위험이 커집니다. 조정이 필요하면 두 가지 방향이 있습니다.

- **A.** `load_days()` 에서 `reader_date` 를 따라가 그 날짜의 독자 데이터를 끌어오고, 화면에 기준일 표기
- **B.** 날짜 선택 기본값을 "독자 데이터가 있는 최신일" 로 변경 (`build_dashboard.py:768`)

원페이지에는 영향이 없습니다 (AdSense 만 사용).

---

## 6. 되돌리는 방법

파일별로 독립적이라 부분 되돌리기도 가능합니다.

```bash
# 이 적용 커밋 찾기
git log --oneline -5

# 전체 되돌리기
git revert <커밋해시>

# 파일 단위로 되돌리기
git checkout <커밋해시>^ -- collect_ga4.py
```

| 변경 | 파일 | 되돌려도 안전한가 |
|---|---|---|
| 참여시간 합산 | `collect_ga4.py` | ✅ 표시에 영향 없음 |
| 7일 평균 기준 | `common.py` | ✅ 오차만 되돌아감 |
| 원페이지 당일 제외 | `build_onepager.py` | ✅ 독립적 |
| GA4 확정본(C안) | `collect_ga4.py`, `analyze.py` | ⚠️ **두 파일을 함께** 되돌려야 함 |

마지막 항목만 주의가 필요합니다. `collect_ga4.py` 만 되돌리면 D-1 파일이 다시 생기는데 `analyze.py` 는 확정본을 찾으므로, 리포트가 D-1 을 무시하고 여전히 D-2 를 씁니다. 반대로 `analyze.py` 만 되돌리면 D-1 파일이 없어 독자 반응이 "데이터 없음" 이 됩니다.

---

## 7. 첫 실행 시 확인 부탁

1. `collect_ga4.py` 가 4일치를 받는지 (로그에 `[GA4] ... 수집 완료` 4줄)
2. `analyze.py` 로그에 `[GA4 치유] 확정본 반영: ...` 가 뜨는지
3. Jandi 리포트 "독자 반응" 에 이탈률이 **30~60% 대**로 나오고 `※ 기준일` 한 줄이 붙는지
4. 원페이지 곡선 끝이 떨어지지 않는지, "실행 이후 평균" 이 올라갔는지 ($85 → $92 부근 예상)

3번이 이번 수정의 핵심 확인 지점입니다.
