# [회신] 데이터 정합성 수정 요청 검토 결과

수신: 정다운 과장
대상: `dongascience-planning/adsense-daily-report`
일자: 2026-07-28

---

## 0. 결론

**4건 전부 독립 검증했고, 진단이 모두 맞습니다.** 수정 방향에 동의합니다.

증상 → 원인 → 코드 라인 → 수정안까지 붙어 있고, "AdSense 수익은 6센트 차이라 손댈 필요 없다"처럼 **건드리지 말 것을 구분해 준 것**이 특히 도움이 됐습니다. 지적대로 **이 리포트의 안전장치(광고를 늘렸을 때 독자가 이탈하는지)가 사실상 작동하지 않는 상태**였습니다.

다만 **1번 B안은 한 줄로 끝나지 않습니다.** 그대로 적용하면 독자 데이터가 빈값이 되는 부작용이 있어, 아래 4장에 대안을 정리했습니다.

---

## 1. 검증 결과 요약

| # | 지적 | 검증 방법 | 결과 |
|---|---|---|---|
| 1 | GA4 미집계로 이탈률 왜곡 | 과거 날짜를 현재 시점에 **재조회**해 저장값과 비교 | ✅ 확증 |
| 2 | 참여시간 합산 오류 | 저장 데이터의 total vs 기기별 합 대조 | ✅ 확증 |
| 3 | 7일 평균이 개수 기준 | `common.py` 코드 + 결측일 확인 | ✅ 확증 |
| 4 | 원페이지가 오늘까지 집계 | `build_onepager.py` 코드 확인 | ✅ 확증 |

### 1번 — 재조회 실측 (가설의 결정적 근거)

지적하신 "시간이 지나면 제값이 나온다"를 API 재조회로 직접 확인했습니다.

| 날짜 | 저장된 값 | **지금 재조회** | 세션 |
|---|---|---|---|
| 7/22 | 99.0% | **37.6%** | 47,950 |
| 7/24 | 98.8% | **41.2%** | 30,026 |
| 7/25 | 98.8% | **35.0%** | 51,104 |
| 7/26 | 42.1% | **42.1%** (동일) | 44,009 |

- 미집계 시점에 받은 날은 **전부 실제값의 2~3배로 부풀려** 저장돼 있었습니다.
- 늦게 받은 7/26은 저장값과 재조회값이 **완전히 일치** → **한 번 확정되면 안정적**입니다.
- 즉 **재조회로 과거 데이터를 복구할 수 있습니다** (6장).

관측된 확정 시점은 **해당 일자가 끝난 뒤 약 19시간** 지점입니다. 따라서 **아침 9시에는 전날(D-1) 독자 지표를 정확히 얻는 것이 구조적으로 불가능**하고, 확정본은 **D-2**가 가장 최신입니다.

### 2번 — 실측

| 날짜 | 저장 total | 기기별 합 | 모바일 단독 |
|---|---|---|---|
| 7/24 | 622,247 | 1,248,515 | **781,322** |
| 7/25 | 1,401,360 | 2,236,905 | **1,910,949** |

전체가 모바일 하나보다 작습니다. 지적하신 대로 합계 지표를 세션 가중평균으로 처리한 결과입니다.

### 3번 — 보완 사항

결측일이 **6/29 외에 6/20도 있습니다** (총 2일). 우선순위가 낮다는 판단에는 동의합니다.

---

## 2. 2번 — 수정 확정 (이견 없음)

`collect_ga4.py` `_aggregate()`:

```python
"user_engagement_duration": sum(d.get("user_engagement_duration", 0) or 0 for d in devices),
```

`bounce_rate` / `avg_session_duration` 은 비율·평균 지표이므로 가중평균 유지가 맞습니다.

---

## 3. 3번 — 수정 확정 (구현만 살짝 조정)

제안하신 로직에 동의합니다. 다만 **파일을 전부 읽고 나서 필터링하면 불필요한 I/O**가 생기므로, 날짜 판정 후 로드하는 형태를 제안합니다.

```python
def load_history(before_date, days=30):
    """before_date 이전(미포함) days일 구간의 일별 레코드를 날짜순으로 반환."""
    cutoff = before_date - timedelta(days=days)
    records = []
    for f in sorted(DATA_DIR.glob("*.json")):
        try:
            d = datetime.strptime(f.stem, "%Y-%m-%d").date()
        except ValueError:
            continue
        if cutoff <= d < before_date:          # 개수 기준 → 날짜 구간 기준
            rec = load_json(f)
            if rec:
                records.append(rec)
    return records
```

`date` / `timedelta` 는 `common.py` 에 이미 import 되어 있습니다.

> 참고: 이 변경으로 `days` 의 의미가 "레코드 개수"에서 "날짜 구간"으로 바뀝니다. 결측일이 있으면 7일 구간의 레코드가 6개일 수 있는데, **그게 정확한 동작**입니다.

---

## 4. 1번 — B안의 문제와 대안 (판단 요청)

### B안을 그대로 적용하면 깨지는 부분

`collect_ga4.py` 한 줄만 D-2로 바꾸면 `ga4-{D-2}.json` 이 생성되는데, 병합 쪽은 여전히 D-1을 찾습니다.

```
analyze.py:465   d = common.yesterday()                    # D-1
analyze.py:38-39 build_record(d) → ga4-{D-1}.json 를 로드   # ← 파일이 없어짐
```

결과:
1. **어제 레코드의 `ga4` 가 빈값** → 리포트에 독자 반응이 아예 안 나옴
2. 어제 이미 저장된 **D-2 레코드는 옛 99% 값 그대로** (재조회분이 반영되지 않음)
3. 대시보드의 **방문당 수익 = `earnings / sessions × 1000`** 이 서로 다른 날짜를 나누게 됨

즉 1번은 `collect_ga4.py` 한 줄이 아니라 **`analyze.py` 병합 로직까지** 함께 봐야 합니다.

> 사소하지만 실행 전 확인 필요: `collect_ga4.py` 는 현재 `common.yesterday()` 만 사용하고 **`datetime` 을 import 하지 않습니다.** 따라서 `date.today() - timedelta(days=2)` 를 그대로 넣으면 `NameError` 가 납니다. `from datetime import date, timedelta` 를 함께 추가해야 합니다. (`build_onepager.py` 의 4번 수정과 `common.py` 의 3번 수정은 필요한 import 가 이미 있어 그대로 적용 가능합니다.)

### 선택지

| 안 | 내용 | 장점 | 단점 |
|---|---|---|---|
| **A** | cron을 저녁(19시 이후)으로 | 코드 변경 없음. 날짜 정합성 유지 | 아침 리포트가 아니게 됨 |
| **B'** | GA4 확정본(D-2)만 저장 + `analyze.py` 병합 조정 | 아침 리포트 유지 | 어제 독자 지표는 표시 못 함 |
| **C** | **B' + 최근 3~4일 재조회 덮어쓰기(자기 치유)** | 아침 유지 + 히스토리 자동 복구 | 구현이 조금 더 있음 |

### 권고: C안

이 리포트의 독자 지표는 **"광고를 늘렸는데 이탈이 늘었나"를 보는 안전장치**이므로, 하루 늦은 확정값이 오히려 적합합니다. 매일 아침 실행 시 최근 며칠을 다시 받아 덮어쓰면 **과거 오류까지 스스로 치유**됩니다.

구현 스케치:

```python
# collect_ga4.py — 확정본(D-2 이후) 수집 + 최근 며칠 재조회
BACKFILL_DAYS = 4          # D-2 ~ D-5

def main():
    property_id = common.env("GA4_PROPERTY_ID", required=True)
    # GA4는 해당 일자 종료 후 약 19시간이 지나야 확정된다.
    # 아침 실행에서 D-1은 미집계 → D-2 이후만 신뢰하고, 최근 며칠은 다시 받아 덮어쓴다.
    for back in range(2, 2 + BACKFILL_DAYS):
        d = date.today() - timedelta(days=back)
        data = fetch(property_id, d)
        common.save_json(common.RAW_DIR / f"ga4-{d.isoformat()}.json",
                         {"date": d.isoformat(), "ga4": data})
```

```python
# analyze.py — 재조회분을 기존 일별 레코드에 반영
def refresh_ga4_in_records(days=4):
    for back in range(2, 2 + days):
        d = common.yesterday() - timedelta(days=back - 1)
        ga = common.load_json(common.RAW_DIR / f"ga4-{d.isoformat()}.json")
        rec = common.load_json(common.daily_path(d))
        if ga and rec:
            rec["ga4"] = ga.get("ga4", {})
            common.save_json(common.daily_path(d), rec)
```

함께 맞춰야 할 표기:
- 리포트의 독자 반응 섹션은 **"기준일: D-2 (확정)"** 임을 명시 (수익은 D-1, 독자 지표는 D-2)
- **방문당 수익은 같은 날짜끼리만** 계산 (D-2 레코드 내부의 earnings ÷ sessions)

**A안(저녁 이동)으로 갈지 C안으로 갈지는 "아침 알림을 유지할지"에 대한 판단이므로, 정다운 과장님·다우 의견에 따르겠습니다.** 저는 C안을 권합니다.

---

## 5. 4번 — 수정 확정 (이견 없음)

제안하신 코드가 맞습니다.

```python
today = date.today()
if (TARGET_YEAR, TARGET_MONTH) == (today.year, today.month):
    cur = {d: v for d, v in cur.items() if d < today.day}
if not cur:
    raise SystemExit("[원페이지] 이번 달 AdSense 데이터가 아직 없습니다.")
as_of = max(cur)
```

`date` 는 `build_onepager.py` 에 이미 import 되어 있고, `daysLeft` · 평균 세그먼트 · 하단 문구는 모두 `as_of` 기반이라 자동으로 따라옵니다.

> **주의: 레이아웃은 `report/` 가 아니라 `templates/광고수익_원페이지.html` 을 수정해야 합니다.** `report/` 는 생성물(gitignore)이고, `build_onepager.py` 가 템플릿의 `/*@DATA_START@*/ … /*@DATA_END@*/` 블록만 교체해 출력합니다.

---

## 6. 과거 데이터 백필 (6/21 ~ 7/25, 약 33일)

재조회가 정상 동작함을 확인했으므로 복구 가능합니다. 다만 `data/*.json` 을 덮어쓰는 작업이라 아래를 전제로 진행하는 것이 안전합니다.

- **AdSense 수치는 건드리지 않고 `ga4` 키만 교체** (수익 금액 불변)
- 실행 전 `data/` 백업 또는 별도 브랜치에서 작업 후 PR
- 백필 후에는 **이탈률 추세선과 과거 인사이트 문구가 바뀝니다** (그게 목적)

---

## 7. 작업 순서 제안

| 순서 | 항목 | 파일 | 비고 |
|---|---|---|---|
| 1 | 참여시간 합산 | `collect_ga4.py` | 이견 없음 |
| 2 | 원페이지 오늘 제외 | `build_onepager.py` | 이견 없음 |
| 3 | 7일 평균 구간 기준 | `common.py` | 이견 없음 |
| 4 | GA4 수집 시점 (A/C 결정 후) | `collect_ga4.py`, `analyze.py` | **판단 필요** |
| 5 | 과거 33일 백필 | 일회성 스크립트 | 4번 확정 후 |

1~3번은 서로 독립적이라 먼저 진행해도 됩니다.

---

## 8. 저장소 인계 참고 사항

작업 전 루트의 **`CLAUDE.md`** 를 먼저 봐주세요 (Claude Code 사용 시 자동 로딩됩니다). 요약하면:

- **최우선 제약**: 광고를 과하게 가려 기사 읽기를 방해하지 않는다 (모바일 인아티클 ≤30%, 이탈률 동시 감시). 이번 수정이 바로 이 감시축을 되살리는 작업입니다.
- **러너 2개 역할 분리**
  - 클라우드 CI (`.github/workflows/daily.yml`, 09:00 KST): 수집 → 분석 → **Jandi 전송** → `data/` 커밋. **Jandi 정본**
  - 로컬 (`run_daily.ps1`, Windows 스케줄러 08:00): 사내 LAN 서빙용 `report/` 생성. **Jandi 전송 안 함** (이중 전송 방지)
- **`send_jandi.py` 는 실제 메시지를 보냅니다.** 테스트 목적으로 단독 실행하지 말아주세요.
- **AdSense 토큰**: 로컬에서 `auth_adsense.py` 로 재인증하면 새 토큰이 발급되므로, GitHub Secret **`TOKEN_ADSENSE_JSON` 도 함께 갱신**해야 합니다. (갱신 누락 시 클라우드가 `invalid_grant` 로 실패합니다 — 실제로 겪었습니다)
- **`ANTHROPIC_API_KEY` Secret 이 없어** 클라우드 인사이트는 현재 규칙 기반으로 생성됩니다. AI 인사이트가 필요하면 Secret 추가가 필요합니다.
- GitHub Pages 는 사용하지 않습니다 (비공개 저장소 + 사내 LAN 서빙). `PAGES_TOKEN`, `GA4_SERVICE_ACCOUNT_JSON` Secret 은 미사용 상태입니다.
- `data/*.json` 은 추세 비교에 쓰이므로 커밋 대상이고, **저장소는 반드시 비공개 유지**(수익 데이터)입니다.

---

정확한 분석 감사합니다. 특히 재조회 시점 비교로 원인을 특정해 주신 부분이 결정적이었습니다.
1번 A/C 결정만 알려주시면 나머지는 순서대로 진행 가능합니다.
